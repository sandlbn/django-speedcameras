============
Installation
============

At the command line::

    $ easy_install django-speedcameras

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-speedcameras
    $ pip install django-speedcameras